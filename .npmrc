; Refuse to install dependency versions published less than 2 weeks (20160 minutes) ago.
; Defense-in-depth against compromised npm publishes — fresh-zero-days don't reach lockfile.
minimum-release-age=20160

; Use the pnpm-native lockfile and isolated node_modules layout.
strict-peer-dependencies=true
auto-install-peers=false
